{
  "name": "tideline",
  "version": "4.2.0",
  "private": true,
  "description": "Stages firmware rollouts to field devices.",
  "scripts": {
    "test": "node --test --experimental-strip-types tests/",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
